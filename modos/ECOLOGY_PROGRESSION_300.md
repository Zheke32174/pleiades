# Pleiades Ecology — Organic Progression Points 201–300

These points were derived from the actual repository state after points 101–200, not from a prewritten million-item backlog. The operator instruction to continue authorizes further **repository-side preparation**. It does not supply private evidence, approve a history rewrite, designate keys or nodes, issue a grant, authorize live execution, or fabricate elapsed observation.

Status vocabulary:

- `implemented` — present on `governance/convergence-proof-v1` as code, contract, deterministic fixture, test, workflow, receipt surface, reviewed documentation, or reproduced aggregate evidence;
- `blocked` — requires real private evidence, sovereign authorization, signing identity, live infrastructure, or elapsed observation;
- `specified` — intentionally reserved for derivation from later real receipts.

## Phase U — Correct checkpoint 200 and retire transition machinery

201. `implemented` Compare the durable branch evidence receipt with the current PR head rather than trusting a narrative summary.
202. `implemented` Preserve the stale failing receipt as evidence instead of relabeling it green.
203. `implemented` Retire the completed all-in-one convergence repair workflow.
204. `implemented` Retire the completed projection-golden refresh workflow.
205. `implemented` Retire the completed adapter/control-plane repair workflow.
206. `implemented` Retire the completed host-profile disclosure repair workflow.
207. `implemented` Retire the completed main-ancestry convergence workflow.
208. `implemented` Remove the remaining current-tree copy of the former workstation identity contained by repair machinery.
209. `implemented` Preserve historical copies only as issue-#42 reachable-history evidence; perform no rewrite.
210. `implemented` Reproduce the complete convergence receipt on the exact post-retirement branch state.

## Phase V — Harden convergence evidence publication

211. `implemented` Extract branch-evidence generation from inline workflow code into a deterministic Python publisher.
212. `implemented` Read only bounded receipts from `artifacts/`.
213. `implemented` Keep wall-clock time out of the evidence identity.
214. `implemented` Preserve privacy-scrubbed failure details when commands fail.
215. `implemented` Bind the evidence to the exact validated commit.
216. `implemented` Ignore the generated `BRANCH_EVIDENCE.json` path to prevent self-trigger loops.
217. `implemented` Cancel stale in-progress validation when a newer branch head appears.
218. `implemented` Publish the receipt before returning a failing workflow status.
219. `implemented` Rebase the evidence-only commit onto the latest feature-branch head without force.
220. `implemented` Keep evidence publication at authority ceiling `none` with no live execution.

## Phase W — Typed operator-input candidate

221. `implemented` Define `OperatorInputCandidate` as a Draft 2020-12 contract.
222. `implemented` Bind every candidate to an exact source commit and explicit creation time.
223. `implemented` Represent private ecology only through an opaque artifact reference and digests.
224. `implemented` Represent authenticated observed inventory only through an opaque artifact reference and digests.
225. `implemented` Represent node/capability inventory only through a live-evidence reference and digests.
226. `implemented` Represent signing identity through public/opaque provenance without private key material.
227. `implemented` Represent history-rewrite authority as an explicit pending, approved, or declined decision.
228. `implemented` Represent delegated-grant work as proposal evidence rather than issuance.
229. `implemented` Represent canary and rollback selection through bounded plan evidence.
230. `implemented` Represent sustained observation through a bounded plan and explicit duration.
231. `implemented` Require exact artifact and receipt digests for every supplied input.
232. `implemented` Require all bindings to remain null for every unsupplied input.
233. `implemented` Require non-public evidence to use opaque `urn:pleiades:` identities.
234. `implemented` Forbid embedded private payloads.
235. `implemented` Forbid private-key material.
236. `implemented` Forbid claims that external side effects already occurred.
237. `implemented` Enumerate the seven permissible preparation outputs.
238. `implemented` Require every candidate to remain proposal-only.
239. `implemented` Require history rewrite, grant issuance, canonical mutation, and live execution flags to remain false.
240. `implemented` Assign the candidate contract to the governed convergence/handoff family.

## Phase X — Operator-input compiler and safety closure

241. `implemented` Add strict duplicate-key JSON loading.
242. `implemented` Add deterministic canonical JSON and SHA-256 identities.
243. `implemented` Validate candidates with a format-aware Draft 2020-12 validator.
244. `implemented` Reject secret-like GitHub, OpenAI, AWS, PEM, and private-key patterns.
245. `implemented` Reject sensitive credential-bearing field names.
246. `implemented` Reject supplied inputs missing any exact binding.
247. `implemented` Reject unsupplied inputs retaining stale bindings.
248. `implemented` Reject non-opaque private, sovereign, or live references.
249. `implemented` Reject approved or declined history decisions without sovereign evidence.
250. `implemented` Reject pending history decisions carrying authorization or plan bindings.
251. `implemented` Reject canary selection without node and canary-plan evidence.
252. `implemented` Reject observation duration without observation-plan evidence.
253. `implemented` Compile private-closure readiness from ecology and inventory bindings.
254. `implemented` Compile history-decision-record readiness.
255. `implemented` Compile node-admission readiness from node and signing evidence.
256. `implemented` Compile grant-issuance proposals without issuing grants.
257. `implemented` Compile canary-admission plans without contacting nodes.
258. `implemented` Compile observation-window plans without starting a clock.
259. `implemented` Compile next-progression readiness only when every required input and decision is bound.
260. `implemented` Emit one deterministic authority-free compilation receipt.

## Phase Y — Templates, goldens, and adversarial intake proof

261. `implemented` Add a deterministic empty candidate-template generator.
262. `implemented` Pre-classify all eight evidence slots without supplying them.
263. `implemented` Leave all private/live artifact references and digests null in the empty template.
264. `implemented` Leave the history decision pending in the empty template.
265. `implemented` Leave canary selection and observation duration unset.
266. `implemented` Request every preparation output without authorizing any.
267. `implemented` Add a synthetic fully bound candidate containing no real private identities.
268. `implemented` Pin its candidate digest.
269. `implemented` Pin all seven generated plan digests.
270. `implemented` Pin the complete compilation receipt digest.
271. `implemented` Test deterministic template generation.
272. `implemented` Test deterministic compilation.
273. `implemented` Test secret-pattern rejection.
274. `implemented` Test cross-binding and sovereign-decision failures.
275. `implemented` Test that both ready and blocked receipts remain non-executing.

## Phase Z — Convergence-bound operator handoff packet

276. `implemented` Define a typed `OperatorHandoffPacket` contract.
277. `implemented` Bind the exact operator candidate digest.
278. `implemented` Bind and reproduce the compilation receipt digest.
279. `implemented` Bind and reproduce the branch convergence evidence digest.
280. `implemented` Bind and reproduce the intervention-frontier receipt digest.
281. `implemented` Require convergence-suite success for review readiness.
282. `implemented` Require a clear current-tree sensitivity receipt.
283. `implemented` Require a passed synthetic rehearsal.
284. `implemented` Require a validation-package receipt that excludes private material.
285. `implemented` Make repository repair outrank operator readiness.
286. `implemented` Preserve blocked and ready plans separately.
287. `implemented` Carry exact operator/live actions from the frontier without executing them.
288. `implemented` Bind every packet to the real numbered runbook anchors.
289. `implemented` Add adversarial stale-candidate, tampered-receipt, repository-failure, and operator-action tests.
290. `implemented` Add intake and packet proofs to the aggregate suite, contract governance, CODEOWNERS, focused PR workflow, and reproducible offline package; reproduce all 35 commands successfully.

## Phase AA — Real operator/live frontier

291. `blocked` Supply authorized opaque references and digests for the actual exhaustive private ecology.
292. `blocked` Supply authorized opaque references and digests for a fresh authenticated observed inventory.
293. `blocked` Approve or decline the issue-#42 public-history rewrite through a real sovereign decision receipt.
294. `blocked` Supply live node identities, capability ceilings, rollback predecessors, and public signing identities.
295. `blocked` Review, sign, and issue the first real delegated authority grant; a proposal is insufficient.
296. `blocked` Select the first real canary and bind its exact rollback plan.
297. `blocked` Authorize and execute the live admission, observation, rollback, and learning loop.
298. `blocked` Authorize a bounded observation duration and allow it to elapse with uninterrupted evidence.
299. `blocked` Review the real outcomes and decide whether authority remains, narrows, suspends, revokes, or grows.
300. `specified` Derive points 301–400 only from the exact final checkpoint-300 receipt plus the later real handoff, execution, rollback, and observation receipts.

## Current interpretation

Points 201–290 are implemented and reproduced by the 35-command aggregate suite. Points 291–299 remain genuinely private, sovereign, live, or time-dependent. Point 300 preserves organic progression: the fourth hundred must be revealed by evidence rather than invented in advance.
